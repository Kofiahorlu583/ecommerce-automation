[pytest]
markers =
    smoke: Smoke test cases (critical functionality)
    regression: Regression test cases (full test suite)

addopts = 
    -v
    --html=reports/report.html
    --self-contained-html

testpaths = tests

python_files = test_*.py
python_classes = Test*
python_functions = test_*